{
  "kind": "build_request",
  "title": "Fix HTTP outcalls import error by replacing missing ExperimentalHttpOutcalls module",
  "projectName": "EcoPowerHub AI",
  "priority": "high",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Replace the non-existent import 'import ExperimentalHttpOutcalls \"mo:base/ExperimentalHttpOutcalls\"' in backend/main.mo (line 11) with the correct HTTP outcalls implementation. The base@0.13.4 package does not contain ExperimentalHttpOutcalls/lib.mo. Use the standard IC management canister approach by importing IC \"mo:base/ExperimentalInternetComputer\" or defining the HTTP outcalls types directly in the actor.",
      "target": "backend",
      "source": {
        "messageIds": [
          "msg-5"
        ],
        "quotes": [
          "/home/bill/ecopowerhub-ai-clean/src/ais_assistant/main.mo:11.1-11.47: import error [M0009], file \".mops/base@0.13.4/src/ExperimentalHttpOutcalls/lib.mo\" does not exist"
        ]
      },
      "acceptanceCriteria": [
        "The import error M0009 is resolved",
        "The Motoko compiler successfully compiles backend/main.mo",
        "HTTP outcalls functionality remains intact for existing features (device API calls, weather API)",
        "dfx deploy ais_assistant completes without compilation errors"
      ]
    },
    {
      "id": "REQ-2",
      "text": "Define the HTTP outcalls types and management canister interface directly in backend/main.mo if they are not available from base library. Include types for HttpRequestArgs, HttpHeader, HttpMethod, HttpResponsePayload, and the management canister interface for http_request method.",
      "target": "backend",
      "source": {
        "messageIds": [
          "msg-5"
        ],
        "quotes": [
          "import error [M0009], file \".mops/base@0.13.4/src/ExperimentalHttpOutcalls/lib.mo\" does not exist"
        ]
      },
      "acceptanceCriteria": [
        "All HTTP outcalls type definitions are present and correct",
        "The management canister actor is properly defined for making HTTP requests",
        "Existing HTTP outcall functionality (setDevice, weather fetching) works as before"
      ]
    },
    {
      "id": "REQ-3",
      "text": "Ensure all existing HTTP outcall usage in backend/main.mo is updated to use the corrected import or inline type definitions. This includes any calls to management canister's http_request function for device APIs and weather data.",
      "target": "backend",
      "source": {
        "messageIds": [
          "msg-5"
        ]
      },
      "acceptanceCriteria": [
        "All HTTP outcall invocations compile successfully",
        "The setDevice function at http://192.168.1.100/rpc/EM.GetStatus?id=0 continues to work",
        "No runtime errors occur when making HTTP outcalls"
      ]
    }
  ],
  "constraints": [
    "Do not change the single-actor architecture",
    "Preserve all existing backend functionality including device management, energy data, alarms, and EV integration",
    "The solution must work with base@0.13.4 dependency as currently installed",
    "Do not modify frontend code"
  ],
  "nonGoals": [
    "Adding new features or functionality",
    "Upgrading to a different version of the base library",
    "Modifying the MOPS package configuration",
    "Creating migration logic"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}